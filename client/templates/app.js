/*
if (Meteor.isClient) {
	angular.module('issuesmod', ['angular-meteor']);
	angular.module("issuesmod").controller("IssuesListController", ['$scope', '$meteorCollection', 
	function($scope, $meteorCollection){
		$scope.issues = $meteorCollection(Issue);
	}]);
}
*/